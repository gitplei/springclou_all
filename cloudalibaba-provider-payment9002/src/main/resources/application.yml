server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置nacos

## 暴露信息端口
management:
  endpoints:
    web:
      exposure:
        include: "*"
